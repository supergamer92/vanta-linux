# Maintainer: Vanta Linux Team
# vanta-fonts: metapackage for Inter + JetBrains Mono.
# Depends on the official Arch packages instead of rebuilding.

pkgname=vanta-fonts
pkgver=1.0
pkgrel=1
pkgdesc="Vanta Linux - Inter and JetBrains Mono font metapackage"
arch=('any')
url="https://vanta.linux"
license=('OFL-1.1')
depends=('ttf-inter' 'ttf-jetbrains-mono')
options=('!strip' '!purge' '!zipman')

package() {
  return 0
}
